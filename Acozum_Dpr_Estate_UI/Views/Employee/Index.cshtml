@model List<ResultEmployeeDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Admin/Index.cshtml";
}
@{int count = 0;}
@{string delyazi = "";}
@{string delicon = "";}
@{string delcolor = "";}
@{string stayazi = "";}
@{string staicon = "";}
@{string stacolor = "";}

<div class="col-12">
    <div class="bg-light rounded h-100 p-4">
        <h6 class="mb-4">Personel Listesi</h6>
        <div class="table-responsive">
            <table class="table text-center">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Personel Adı Soyadı</th>
                        <th scope="col">Ünvan</th>
                        <th scope="col">Durum</th>
                        <th scope="col">Sil</th>
                        <th scope="col">Aktif/Pasif Yap</th>
                        <th scope="col">Güncelle</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        count++;
                        switch (item.Status)
                        {
                            case true:
                                delyazi = "Pasif Yap";
                                delicon = "fa fa-lock";
                                delcolor = "btn btn-danger";
                                stacolor = "btn btn-success";
                                staicon = "fa fa-lock-open";
                                stayazi = "Aktif";
                                break;
                            case false:
                                delyazi = "Aktif Yap";
                                delicon = "fa fa-lock-open";
                                delcolor = "btn btn-success";
                                stacolor = "btn btn-danger";
                                staicon = "fa fa-lock";
                                stayazi = "Pasif";
                                break;
                        }
                        <tr>
                            <th scope="row">@count</th>
                            <td>@item.Name</td>
                            <td>@item.Title</td>
                            <td><span class="@stacolor">@stayazi <i class="@staicon"></i></span></td>
                            <td><a href="/Employee/DeleteEmployee/@item.EmployeeID" class="btn btn-outline-danger">Sil</a></td>
                            <td><a href="/Employee/ChangeStatusEmployee/@item.EmployeeID" class="@delcolor"><i class="@delicon"></i> @delyazi</a></td>
                            <td><a href="/Employee/UpdateEmployee/@item.EmployeeID" class="btn btn-outline-primary">Güncelle</a></td>

                        </tr>
                    }
                </tbody>
            </table>
            <a href="/Employee/CreateEmployee/" class="btn btn-outline-info">Yeni Personel Girişi</a>
        </div>
    </div>
</div>

